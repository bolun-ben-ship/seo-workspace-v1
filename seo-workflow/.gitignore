# Credentials and secrets — never commit these
*.json
!package.json

# Google service account keys
*service-account*.json
*credentials*.json
*-key.json
google-*.json

# Environment files
.env
.env.*
*.env

# API key files
api-keys.*
secrets.*

# macOS
.DS_Store
**/.DS_Store

# Output files (generated by skills — workspace-specific, not part of the repo)
outputs/
Content & SEO/

# Logs
*.log
